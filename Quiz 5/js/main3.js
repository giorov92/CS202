$(document).ready(function(){

    // $('slider').cycle();
    $('.slider').cycle({
        //fx: 'scrollHorz'
        fx: 'scrollVert',
        next: 'next'

        //wipe, cover, uncover, slideX, slideY, shuffle
    });
})